# SolarMail REST API - Requirements
# Sprint 0.3.2: FastAPI dependencies

# FastAPI и ASGI сервер
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Pydantic для валидации
pydantic>=2.5.0
pydantic-settings>=2.1.0

# System monitoring
psutil>=5.9.0

# CORS support (встроено в FastAPI, но указываем явно)
python-multipart>=0.0.6

# ==================== AI Integration ====================
# Зависимости из core/sync для AIParserTransformer
# (должны быть установлены отдельно или через ../core/sync/requirements.txt)

# transformers>=4.30.0
# torch>=2.0.0
# sentencepiece>=0.1.99

# ==================== Optional ====================
# JWT аутентификация (для будущих спринтов)
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.4

# Rate limiting (для будущих спринтов)
# slowapi>=0.1.9

# Async HTTP client
# httpx>=0.25.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # для тестирования API
